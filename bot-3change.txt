pip install steampy
pip install pyotp
pip install beautifulsoup4


import asyncio
import pyotp
import time
from datetime import datetime, timedelta
from typing import Optional, Dict, Tuple
import logging
from steampy.client import SteamClient
from steampy.models import GameOptions, Currency
from steampy.exceptions import ApiException, LoginRequired

logger = logging.getLogger(__name__)

class SteamAccountManager:
    """Менеджер для управления Steam аккаунтами"""
    
    def __init__(self, db_pool):
        self.pool = db_pool
        self.active_sessions = {}  # account_id -> SteamClient
        self.totp_cache = {}       # account_id -> текущий код (кэш)
        
    def generate_totp_code(self, shared_secret: str) -> str:
        """
        Генерация текущего кода Steam Guard
        Используем pyotp для TOTP
        """
        try:
            totp = pyotp.TOTP(shared_secret)
            code = totp.now()
            logger.debug(f"Сгенерирован TOTP код: {code}")
            return code
        except Exception as e:
            logger.error(f"Ошибка генерации TOTP: {e}")
            return None
    
    async def get_account_info(self, account_id: int) -> Optional[Dict]:
        """Получить информацию об аккаунте из БД"""
        async with self.pool.acquire() as conn:
            row = await conn.fetchrow(
                "SELECT * FROM steam_accounts WHERE id = $1",
                account_id
            )
            return dict(row) if row else None
    
    async def get_available_accounts(self, funpay_suffix: str = None) -> list:
        """Получить список доступных аккаунтов"""
        query = "SELECT * FROM steam_accounts WHERE status = 'available'"
        params = []
        
        if funpay_suffix:
            query += " AND funpay_suffix = $1"
            params.append(funpay_suffix)
            
        async with self.pool.acquire() as conn:
            rows = await conn.fetch(query, *params)
            return [dict(row) for row in rows]
    
    async def rent_account(self, account_id: int, renter_info: Dict) -> bool:
        """
        Аренда аккаунта
        renter_info: {'funpay_chat_id': '...', 'funpay_suffix': 'e', 'rent_days': 7}
        """
        async with self.pool.acquire() as conn:
            # Проверяем, доступен ли аккаунт
            account = await conn.fetchrow(
                "SELECT * FROM steam_accounts WHERE id = $1 AND status = 'available'",
                account_id
            )
            
            if not account:
                logger.error(f"Аккаунт {account_id} недоступен для аренды")
                return False
            
            # Рассчитываем время аренды
            rent_days = renter_info.get('rent_days', 1)
            rent_start = datetime.now()
            rent_expires = rent_start + timedelta(days=rent_days)
            
            # Обновляем статус аккаунта
            await conn.execute("""
                UPDATE steam_accounts 
                SET status = 'rented',
                    current_renter = $1,
                    funpay_chat_id = $2,
                    rent_start = $3,
                    rent_expires = $4
                WHERE id = $5
            """, 
                renter_info.get('funpay_user_id', 'unknown'),
                renter_info.get('funpay_chat_id'),
                rent_start,
                rent_expires,
                account_id
            )
            
            # Логируем операцию
            await conn.execute("""
                INSERT INTO steam_operations (account_id, operation_type, operator, details)
                VALUES ($1, 'rent', 'system', $2)
            """, account_id, json.dumps(renter_info))
            
            logger.info(f"✅ Аккаунт {account['login']} арендован до {rent_expires}")
            return True
    
    async def generate_credentials_message(self, account_id: int) -> Dict:
        """
        Генерирует сообщение с данными для входа
        Возвращает логин, пароль и текущий TOTP код
        """
        account = await self.get_account_info(account_id)
        if not account:
            return None
        
        # Генерируем актуальный TOTP код
        totp_code = None
        if account.get('shared_secret'):
            totp_code = self.generate_totp_code(account['shared_secret'])
        
        # Кэшируем код для быстрой выдачи
        self.totp_cache[account_id] = {
            'code': totp_code,
            'generated_at': datetime.now()
        }
        
        return {
            'login': account['login'],
            'password': account['password'],
            'totp_code': totp_code,
            'expires_at': account['rent_expires']
        }
    
    async def force_logout(self, account_id: int) -> bool:
        """
        Принудительный выход с аккаунта
        Меняем пароль и/или отзываем токены
        """
        account = await self.get_account_info(account_id)
        if not account:
            return False
        
        try:
            # Создаем клиент Steam для этого аккаунта
            steam_client = SteamClient(account['login'], account['password'])
            
            # Генерируем код для входа
            totp_code = self.generate_totp_code(account['shared_secret'])
            
            # Логинимся
            steam_client.login(totp_code)
            
            # Меняем пароль
            new_password = self._generate_random_password()
            steam_client.change_password(account['password'], new_password)
            
            # Обновляем пароль в БД
            async with self.pool.acquire() as conn:
                await conn.execute("""
                    UPDATE steam_accounts 
                    SET password = $1, status = 'available', 
                        current_renter = NULL, funpay_chat_id = NULL,
                        rent_start = NULL, rent_expires = NULL
                    WHERE id = $2
                """, new_password, account_id)
                
                await conn.execute("""
                    INSERT INTO steam_operations (account_id, operation_type, operator, details)
                    VALUES ($1, 'force_close', 'system', $2)
                """, account_id, json.dumps({'old_password': account['password'], 'new_password': new_password}))
            
            logger.info(f"✅ Аккаунт {account['login']} принудительно закрыт, пароль изменен")
            return True
            
        except Exception as e:
            logger.error(f"❌ Ошибка при принудительном выходе {account['login']}: {e}")
            return False
    
    def _generate_random_password(self, length=16) -> str:
        """Генерация случайного пароля"""
        import random
        import string
        chars = string.ascii_letters + string.digits + "!@#$%^&*"
        return ''.join(random.choice(chars) for _ in range(length))
    
    async def check_expired_rentals(self):
        """Проверка и закрытие просроченных аренд"""
        async with self.pool.acquire() as conn:
            # Находим просроченные аренды
            expired = await conn.fetch("""
                SELECT id, login FROM steam_accounts 
                WHERE status = 'rented' 
                AND rent_expires < NOW()
            """)
            
            for account in expired:
                logger.info(f"⏰ Просрочена аренда аккаунта {account['login']}")
                await self.force_logout(account['id'])
                
                # Отправляем уведомление арендатору (будет реализовано позже)
                # await self.notify_renter(account['id'], 'expired')
    
    async def extend_rental(self, account_id: int, extra_days: int) -> bool:
        """Продление аренды"""
        async with self.pool.acquire() as conn:
            result = await conn.execute("""
                UPDATE steam_accounts 
                SET rent_expires = rent_expires + ($1 || ' days')::INTERVAL
                WHERE id = $2 AND status = 'rented'
                RETURNING id
            """, extra_days, account_id)
            
            if result:
                logger.info(f"✅ Аренда аккаунта {account_id} продлена на {extra_days} дней")
                return True
            return False
    
    async def get_rental_info(self, account_id: int) -> Optional[Dict]:
        """Получить информацию о текущей аренде"""
        async with self.pool.acquire() as conn:
            row = await conn.fetchrow("""
                SELECT login, current_renter, rent_start, rent_expires 
                FROM steam_accounts 
                WHERE id = $1 AND status = 'rented'
            """, account_id)
            
            if row:
                return {
                    'login': row['login'],
                    'renter': row['current_renter'],
                    'started': row['rent_start'],
                    'expires': row['rent_expires'],
                    'remaining_days': (row['rent_expires'] - datetime.now()).days
                }
            return None










СТИМ


# Добавляем в начало файла бота
from steam_manager import SteamAccountManager
import json

class MultiFunPayBot:
    def __init__(self, dsn: str):
        self.dsn = dsn
        self.pool = None
        self.accounts: Dict[str, FunPayAccount] = {}
        self.steam_manager = None  # Будет инициализирован после БД
        self.tasks = []
        
    async def init_db(self):
        """Инициализация пула соединений с БД"""
        self.pool = await asyncpg.create_pool(self.dsn)
        logger.info("✅ База данных подключена")
        
        # Инициализируем Steam менеджер
        self.steam_manager = SteamAccountManager(self.pool)
        logger.info("✅ Steam менеджер инициализирован")
        
        # Запускаем фоновую задачу проверки аренд
        asyncio.create_task(self.check_rentals_task())
        
        # Загружаем аккаунты FunPay
        await self.load_accounts()
    
    async def check_rentals_task(self):
        """Фоновая задача для проверки и закрытия просроченных аренд"""
        while True:
            try:
                await asyncio.sleep(60)  # Проверка каждую минуту
                await self.steam_manager.check_expired_rentals()
            except Exception as e:
                logger.error(f"Ошибка в check_rentals_task: {e}")
    
    def get_auto_response(self, account: FunPayAccount, message_text: str) -> Optional[str]:
        """Расширенный автоответчик с поддержкой команд для аренды"""
        
        # Команда для получения списка доступных аккаунтов
        if '!аккаунты' in message_text or '!accounts' in message_text:
            # Здесь нужно будет получить аккаунты для этого суффикса
            # Пока заглушка
            return "📋 Список доступных аккаунтов можно посмотреть по ссылке: [ссылка на лот]"
        
        # Команда для проверки статуса аренды
        if '!статус' in message_text or '!status' in message_text:
            return "🔍 Введите ID вашей аренды или логин аккаунта для проверки статуса."
        
        # Команда для продления аренды
        if '!продлить' in message_text or '!extend' in message_text:
            return "⏰ Для продления аренды напишите администратору с указанием ID аккаунта."
        
        # Проверка на команды администратора (можно добавить позже)
        if message_text.startswith('!админ'):
            return self.handle_admin_command(account, message_text)
        
        # Стандартные ответы (как в предыдущей версии)
        greetings = ['привет', 'здравствуйте', 'добрый', 'hello', 'hi']
        if any(greet in message_text for greet in greetings):
            return self.get_greeting_message(account)
        
        if '!help' in message_text or '!помощь' in message_text:
            return self.get_help_message(account)
        
        return None
    
    def get_greeting_message(self, account: FunPayAccount) -> str:
        """Приветственное сообщение с информацией о доступных аккаунтах"""
        return f"""Приветствую! Я бот для аренды Steam аккаунтов {account.suffix.upper()} 🎉

📋 **Доступные аккаунты:** 
Список можно посмотреть в лотах или по команде !аккаунты

💰 **Цена аренды:** Уточняйте в лоте
⏰ **Срок аренды:** От 1 до 30 дней
🎮 **Steam Guard:** Код выдается автоматически после оплаты

💡 **Команды:**
!аккаунты - список доступных аккаунтов
!помощь - все доступные команды
!админ - связаться с администратором

✅ Для аренды просто оплатите лот, и бот выдаст данные автоматически!"""
    
    def get_help_message(self, account: FunPayAccount) -> str:
        """Сообщение со всеми командами"""
        return f"""📚 **Доступные команды (аккаунт {account.suffix.upper()}):**

**Общие:**
!help / !помощь - это сообщение
!аккаунты / !accounts - список доступных Steam аккаунтов

**Аренда:**
После оплаты лота бот автоматически выдаст:
• Логин и пароль от аккаунта
• Актуальный код Steam Guard

**Поддержка:**
!админ - связаться с администратором
!статус - проверить статус аренды
!продлить - продлить аренду (требуется подтверждение админа)

**Важно:**
• Код Steam Guard обновляется каждые 30 секунд
• По истечении аренды доступ к аккаунту будет автоматически закрыт
• Для продления обратитесь к администратору не позднее, чем за 24 часа до окончания

👥 Администратор онлайн: 10:00 - 19:00 МСК"""
    
    def handle_admin_command(self, account: FunPayAccount, message: str) -> str:
        """Обработка команд администратора"""
        # Здесь можно добавить проверку, является ли пользователь админом
        # Пока просто пересылаем уведомление
        parts = message.split()
        
        if len(parts) > 1 and parts[1] == 'выдать':
            # Администратор может выдать аккаунт вручную
            return self.handle_manual_rent(account, message)
        
        return "👥 Запрос передан администратору. Ожидайте ответа в рабочее время."
    
    async def handle_payment_confirmation(self, account_suffix: str, funpay_chat_id: str, lot_id: str):
        """
        Обработка подтверждения оплаты
        Вызывается из WebHook или при получении уведомления об оплате
        """
        # Находим свободный аккаунт для этого лота
        async with self.pool.acquire() as conn:
            steam_account = await conn.fetchrow("""
                SELECT * FROM steam_accounts 
                WHERE status = 'available' 
                AND funpay_lot_id = $1
                LIMIT 1
            """, lot_id)
        
        if not steam_account:
            logger.error(f"Нет доступных аккаунтов для лота {lot_id}")
            await self.send_message_by_suffix(account_suffix, funpay_chat_id, 
                "❌ Извините, свободных аккаунтов сейчас нет. Обратитесь к администратору.")
            return
        
        # Арендуем аккаунт
        renter_info = {
            'funpay_chat_id': funpay_chat_id,
            'funpay_suffix': account_suffix,
            'rent_days': 1  # По умолчанию 1 день, можно брать из лота
        }
        
        success = await self.steam_manager.rent_account(steam_account['id'], renter_info)
        
        if success:
            # Генерируем данные для входа
            credentials = await self.steam_manager.generate_credentials_message(steam_account['id'])
            
            # Формируем сообщение с данными
            message = f"""✅ **Оплата получена! Ваш аккаунт готов.**

🎮 **Данные для входа:**
Логин: {credentials['login']}
Пароль: {credentials['password']}
Steam Guard: {credentials['totp_code']}


